# ── Web framework ─────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ── Database ──────────────────────────────────────────────────────────
sqlalchemy==2.0.36
psycopg2-binary==2.9.12
alembic==1.13.1

# ── Validation ────────────────────────────────────────────────────────
pydantic[email]==2.10.3
pydantic-settings==2.7.0

# ── Auth ──────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── Rate limiting ─────────────────────────────────────────────────────
slowapi==0.1.9

# ── ML (Phase 7 — real trained models) ───────────────────────────────
# Install with: pip install -e ./mkt_surveillance_ml
mkt-surveillance-ml

# ── Phase 8: Streaming ingestion & live data ──────────────────────────
redis==5.0.8              # Redis Streams client
websockets==12.0          # Raw WebSocket client for Binance/Bybit public feeds
aiohttp==3.10.5           # Async HTTP for REST fallbacks

# Crypto feeds
python-binance==1.0.19    # Binance WebSocket & REST (Primary crypto)
# Bybit: uses raw websockets (public feed needs no library)

# US equities
alpaca-py==0.26.1         # Alpaca WebSocket & REST (Primary US equities)
finnhub-python==2.4.19    # Finnhub REST + WebSocket (US fallback & news)

# Indian equities
upstox-python-sdk==2.13.0 # Upstox WebSocket & REST (Primary India)

# Sentiment / News
praw==7.7.1               # Reddit API (Python Reddit API Wrapper)

# Phase 9: AI Report Generation
google-generativeai==0.8.3  # Google Gemini API for Auto-MAR PDF drafts

# Phase 8: Explainability
shap==0.46.0              # SHAP feature-importance for anomaly explanations

# ── Testing ───────────────────────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0
httpx==0.27.0
testcontainers[postgresql]==4.5.1

# ── Utilities ─────────────────────────────────────────────────────────
python-dotenv==1.0.1
